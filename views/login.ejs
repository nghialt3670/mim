<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <link rel="icon" href="/images/logo.ico" type="image/x-icon">
  <link rel="shortcut icon" href="/images/logo.ico" type="image/x-icon">
  <title>Login To Mim</title>
</head>
<body>
  <div class="container">
    <form action="/login" method="post" id="login-form" class="form">
      <h2>Welcome back!</h2>
      <input type="text" id="username" name="username" placeholder="Username" required>
      <% if (username_not_found) { %>
        <div id="username-not-found" class="error-message">Username not found</div>
      <% } %>
      <input type="password" id="password" name="password" placeholder="Password" required>
      <% if (wrong_password) { %>
        <div id="username-not-found" class="error-message">Wrong password</div>
      <% } %>
      <button type="submit" class="login-button">Login</button>
      <p class="login-register-redirect">Don't have an account? Let's <a href="/register">create a new account</a></p>
    </form>
  </div>
</body>
</html>
